/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.discordapp.com", "avatars.githubusercontent.com", "res.cloudinary.com"],
  },
};

module.exports = nextConfig;
